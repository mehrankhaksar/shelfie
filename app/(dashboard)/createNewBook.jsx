import { StyleSheet, Text } from "react-native";
import ThemedView from "../../components/ThemedView";

const CreateNewBook = () => {
  return (
    <ThemedView safe>
      <Text>CreateNewBook</Text>
    </ThemedView>
  );
};

export default CreateNewBook;

const styles = StyleSheet.create({});
